with Triangulo_Model; use Triangulo_Model;
with Figura; use Figura;
with Ada.Text_IO; use Ada.Text_IO;
with Ada.Float_Text_IO; use Ada.Float_Text_IO;


procedure Main is
   Triangulo1 : Triangulo := Create(6.0, 2.0);
begin
   --  Insert code here.
   -- Put(Triangulo.Calcular_Area);
   null;
end Main;
